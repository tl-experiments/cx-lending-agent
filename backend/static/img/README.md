# Tour screenshots

The `/tour` page (Implementation → **Screens**) embeds these four images. Drop PNGs here with
these exact names; the page references them at `/static/img/<name>`:

| File | What it shows |
|---|---|
| `ces-console.png` | The Google CES / CXAS Agent Studio console with the app `tilicho-credit-scrapi` (needs an authenticated browser). |
| `chat.png` | The borrower chat (`/`) — e.g. an EMI lookup + foreclosure quote, with the tool badges. |
| `insights.png` | The Conversational Insights dashboard (`/insights`). |
| `cx-insights.png` | The native Contact Center AI Insights console — conversations with sentiment + Quality AI scores (authenticated browser). |

Until a file is present, the page shows the image's alt text in its place (no layout break).
Recommended capture width ~1300px for a crisp, consistent look.

## Brand

| File | What it shows |
|---|---|
| `tilicho-logo.webp` | Official Tilicho Labs logo lockup (red mark + white "tilicho labs"), transparent bg. Used in the dark page headers (`/`, `/assist`, `/tour`). The light `/insights` header uses an inline dark-text version so the wordmark stays legible. |
